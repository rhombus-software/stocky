[project]
name = "stocky-automation-v1"
version = "0.1.0"
description = "Stocky automation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "pandas",
    "supabase",
    "yfinance",
]
